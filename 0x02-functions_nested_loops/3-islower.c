#include "main.h"

/**
 * _islower - checks if a character c is lowercase
 * Return: 1 if lowercase and 0 otherwise
 */
int _islower(int c)
{
	int i;

	for (i = 'a'; i <= 122; i++)
	{
		_putchar("1");
	}
return (0);
