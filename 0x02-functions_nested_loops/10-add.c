#include "main.h"

/**
 * add - function that adds two integers
 *
 * assigned integers to be added
 *
 * Return: the summed up result
 */
int add(int i, int j)
{
	int r;

	r = i + j;
	_putchar(r + '0');
	return (r);
}

