#include <stdlib.h>
#include <stdio.h>
#include <unistd.h>

/**
 * main - prints alphabets in lowercase
 * Return: Always 0 (success)
 */
int main(void)
{
	char a;

	for (a = 97; a < 123; a++)
	{
		putchar("a");
	}
	return (0);
}
